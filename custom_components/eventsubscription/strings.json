{
  "title": "EventSubscription",
  "config": {
    "step": {
      "user": {
        "title": "[%key:component::eventsubscription::title%]",
        "description": "Do you want to configure the EventSubscription?"
      }
    },
    "abort": {
      "already_configured": "EventSubscription is already configured"
    }
  }
}